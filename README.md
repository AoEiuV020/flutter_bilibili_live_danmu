# Flutter Bilibili Live Danmu

哔哩哔哩直播弹幕应用 - 基于 B站直播开放平台 API

## 项目结构

本项目使用 **Melos** 管理多模块工作空间：

```
flutter_bilibili_live_danmu/
├── apps/                           # 应用目录
│   └── bilibili_live_danmu/       # 主应用：弹幕显示界面
├── packages/                       # 公共包目录
│   └── bilibili_live_api/         # B站直播开放平台 API 封装
└── pubspec.yaml                    # 工作空间配置
```

## 模块说明

### 📦 packages/bilibili_live_api

B站直播开放平台 API 客户端封装包。

**特点：**
- 使用官方 API，而非 Web 破解。
- 需要直播间主人的身份码（而非房间号）进行操作。

**功能：**
- 完整的鉴权签名机制（HMAC-SHA256）
- 调用 `start` 接口以建立 WebSocket 连接接收消息
- 心跳维持
- 自动错误处理

**文档：** [packages/bilibili_live_api/README.md](packages/bilibili_live_api/README.md)

### 📱 apps/bilibili_live_danmu

直播弹幕显示应用。

**特点：**
- 专为小透明直播 UP 主设计，避免错过有人进房间和点赞。
- 支持语音播报所有消息（不仅仅是弹幕）。

**功能：**
- 配置管理（App ID、密钥等）
- 实时弹幕显示
- 消息自动过期
- 全屏黑色背景(OLED友好)

**文档：** [apps/bilibili_live_danmu/README.md](apps/bilibili_live_danmu/README.md)

## 快速开始

### 1. 安装依赖

```bash
# 使用 melos（推荐）
melos bootstrap

# 或者使用 flutter
flutter pub get
```

### 2. 运行应用

```bash
cd apps/bilibili_live_danmu
flutter run
```

## 许可证

MIT License

## 平台支持

本项目目前支持以下平台：

- **macOS**：已测试
- **Android**：已测试
- **Web**：连接后端代理http请求避免跨域问题，已测试
- **Windows**/**Linux**/**iOS**：暂不维护
